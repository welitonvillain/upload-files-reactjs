import { Uploader } from "../../components/Uploader"
import { Container } from "./styles"

export const Main: React.FC = () => {
    return (
        <Container>
            <Uploader />
        </Container>
    );
};